{% comment %}
  Shugroves Emporium - Popular & Bestselling Products Section
{% endcomment %}

<div class="section-container" id="popularSection">
  <!-- Section Editorial Header -->
  <div class="section-header">
    <div class="section-header-left">
      <span class="section-eyebrow">{{ section.settings.eyebrow | default: "Atelier Favorites" }}</span>
      <h2 class="section-heading">{{ section.settings.heading | default: "Popular Pieces" }}</h2>
    </div>
    <div class="section-header-right">
      {%- assign col_link = section.settings.collection.url | default: routes.all_products_collection_url -%}
      <a href="{{ col_link }}?sort_by=best-selling" class="btn-text-link">
        {{ section.settings.view_all_text | default: "Explore All Bestsellers &rarr;" }}
      </a>
    </div>
  </div>

  <!-- Product Cards Grid -->
  {%- assign collection = section.settings.collection -%}
  {%- assign products_to_display = collection.products | default: collections.all.products -%}
  
  <div class="product-grid product-grid-4">
    {%- if products_to_display != blank and products_to_display.size > 0 -%}
      {%- for product in products_to_display limit: section.settings.products_to_show -%}
        {% render 'product-card', product: product %}
      {%- endfor -%}
    {%- else -%}
      <div class="empty-state-card" style="grid-column: 1 / -1; text-align: center; padding: 4rem 2rem; background: var(--bg-cream-tint); border: 1px solid var(--border-subtle); border-radius: 16px;">
        <h3 class="empty-state-title" style="font-family: var(--font-serif); font-size: 1.6rem; color: var(--text-charcoal); margin-bottom: 0.5rem;">No popular creations available</h3>
        <p class="empty-state-desc" style="color: var(--text-charcoal-muted); max-width: 480px; margin: 0 auto 1.5rem; font-size: 0.9rem;">Bestselling pieces from your store will automatically be curated here.</p>
        <a href="{{ routes.all_products_collection_url }}" class="btn-primary" style="display: inline-block; padding: 0.8rem 1.8rem; border-radius: 24px;">Explore Core Archive</a>
      </div>
    {%- endif -%}
  </div>
</div>

{% schema %}
{
  "name": "Popular Pieces",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow Text",
      "default": "Atelier Favorites"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Popular Pieces"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Popular Collection (or leave blank for all products)"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 4,
      "max": 16,
      "step": 4,
      "default": 4,
      "label": "Number of products to show"
    },
    {
      "type": "text",
      "id": "view_all_text",
      "label": "View All Link Text",
      "default": "Explore All Bestsellers →"
    }
  ],
  "presets": [
    {
      "name": "Popular Pieces"
    }
  ]
}
{% endschema %}
